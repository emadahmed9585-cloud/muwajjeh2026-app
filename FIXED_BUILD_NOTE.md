# Fixed APK build

This package preserves the original Muwajjeh lib/ source while generating only the Android platform in /tmp.
The workflow no longer runs `flutter create ... .` in the project root.
